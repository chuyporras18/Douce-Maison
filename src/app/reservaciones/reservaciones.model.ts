//Interfaz del modelo de la reservacion creada por el usuario.
export interface Reservaciones {
  nombre: string;
  correo: string;
  telefono: string;
  fechaI: string;
  fechaF: string;
  icono: string;
  titulo: string;
  id: string;
  total: number;
  userId: string;
}
